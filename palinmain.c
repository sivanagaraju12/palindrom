#include <stdio.h>
#include"header.h"
int main()
{
    char str[20];
    int i, len, temp=0;
    int flag = 0;
    printf("Enter a string:");
    scanf("%s", str);  
 
if (temp==0) 
{
        printf("String is a palindrome");
    }
    else
    {
        printf("String is a notPalindrome");
    } 
    }
